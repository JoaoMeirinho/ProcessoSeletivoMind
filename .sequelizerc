const { resolve } = require("path");

module.exports = {
  config: resolve(__dirname, "services", "database", "dbConnection.js"),
  "models-path": resolve(__dirname, "src", "models"),
  "migrations-path": resolve(__dirname, "services", "database", "migrations"),
  "seeders-path": resolve(__dirname, "services", "database", "seeds"),
};
